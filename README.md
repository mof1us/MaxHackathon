# MAX Hackathon - Бот для расписания университета

Монорепозиторий, содержащий систему управления расписанием университета, созданную для MAX Hackathon. Этот проект предоставляет комплексное решение для управления и отображения расписаний университета через интерфейс бота MAX API.

## 🏗️ Архитектура

Этот монорепозиторий состоит из нескольких микросервисов, работающих вместе:

- **Frontend Bot** - Бот на основе MAX API для взаимодействия с пользователями
- **Schedule Backend** - Сервис на Go для управления расписанием и парсинга ICS файлов
- **Image Generation Backend** - Сервис на Python для генерации изображений расписания
- **Universities Backend** - Сервис на Python для управления данными университетов
- **PostgreSQL Database** - Общая база данных для всех сервисов

## 📁 Структура проекта

```
.
├── frontend/                    # MAX API Bot (Python)
│   ├── bot/                    # Логика бота и маршрутизация
│   ├── database/               # Сущности и сервисы базы данных
│   ├── data_types/            # Модели данных
│   ├── maxapi/                # MAX API SDK
│   └── main.py                # Точка входа
│
├── schedule_back/              # Сервис расписания (Go)
│   ├── cmd/                   # Точки входа приложения
│   ├── internal/              # Внутренние пакеты
│   │   ├── config/           # Конфигурация
│   │   ├── domain/           # Доменные модели
│   │   ├── parser/           # Парсер ICS
│   │   ├── repo/             # Слой репозитория
│   │   ├── service/          # Бизнес-логика
│   │   └── transport/        # HTTP обработчики
│   └── migrations/           # Миграции базы данных
│
├── image_generation_backend/   # Сервис генерации изображений (Python)
│   ├── application/          # Логика приложения
│   └── template_generator/   # Рендеринг шаблонов
│
├── universities_backend/      # Сервис университетов (Python)
│   ├── application/          # Логика приложения
│   └── database/             # Слой базы данных
│
├── postgres_init/            # Скрипты инициализации базы данных
│   └── init.sql             # Создание базы данных
│
└── docker-compose.yml        # Docker Compose
```

## 🚀 Быстрый старт

### Требования

- Docker и Docker Compose
- Токен MAX API

### Запуск приложения

1. **Клонируйте репозиторий**
   ```bash
   git clone <repository-url>
   cd MaxHackathon
   ```

2. **Настройте переменные окружения**

   Создайте файл `.env` с необходимыми полями(пример можно посмотрерть в `.env-example`):


3. **Запустите все сервисы**
   ```bash
   docker-compose up
   ```

   Это запустит:
   - Базу данных PostgreSQL
   - Frontend бот
   - Schedule backend
   - Сервис генерации изображений
   - Universities backend
4. **ics файлы.**
   
    Для работы бота необходимо загружать расписания в формате ics. Вот пример такого файла:
    ```https://table.nsu.ru/ics/group/24942```
## 🔧 Обзор сервисов

### Frontend Bot (`frontend/`)

Бот на Python, построенный на фреймворке MAX API, который предоставляет:
- Интерактивную систему меню для управления расписанием
- Отображение расписания (недельный/дневной вид)
- Загрузку и обработку ICS файлов
- Поиск и обмен расписаниями
- Управление состоянием пользователя

**Технологии:**
- Python 3.x
- FastAPI
- MAX API
- PostgreSQL (через psycopg2)
- aiohttp

### Schedule Backend (`schedule_back/`)

Сервис на Go, который обрабатывает:
- CRUD операции с расписаниями
- Парсинг ICS файлов
- Управление элементами расписания
- Связи пользователь-расписание
- Управление токенами генерации изображений расписания

**Технологии:**
- Go 1.25
- Gin (HTTP фреймворк)
- GORM (ORM)
- Драйвер PostgreSQL

### Image Generation Backend (`image_generation_backend/`)

Сервис на Python, который генерирует визуальные представления расписаний:
- Изображения недельного расписания
- Изображения дневного расписания
- Рендеринг HTML шаблонов
- Генерация скриншотов изображений
- загрузка изображекний на сервера MAX и оперирование его токенами

**Технологии:**
- Python 3.x
- FastAPI
- Jinja2 (шаблонизация)
- html2image
- aiohttp

### Universities Backend (`universities_backend/`)

Сервис на Python для управления данными университетов:
- CRUD операции с университетами
- Поиск университетов
- Сохранение в базе данных

**Технологии:**
- Python 3.x
- FastAPI
- PostgreSQL (через psycopg2)


## 🔌 Взаимодействие API

Сервисы взаимодействуют через внутреннюю Docker сеть:

- Frontend Bot → Schedule Backend: `http://schedule_backend:8010/api/`
- Frontend Bot → Universities Backend: `http://university_backend:8001/`
- Schedule Backend → Image Generator: `http://image-generator:8000/`

## 📝 Возможности

- ✅ Управление расписанием (добавление, просмотр, список)
- ✅ Парсинг и импорт ICS файлов
- ✅ Поиск и управление университетами
- ✅ Обмен расписаниями через токены
- ✅ Визуальная генерация расписания (недельный/дневной вид)
- ✅ Интерактивный интерфейс бота
- ✅ Управление состоянием пользователя
